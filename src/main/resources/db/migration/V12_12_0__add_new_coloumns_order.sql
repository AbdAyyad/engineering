alter table eng_order
    add column company varchar(256);

alter table eng_order
    add column company_cat varchar(256);

alter table eng_order
    add column email varchar(256);

alter table eng_order
    add column sec_phone varchar(256);